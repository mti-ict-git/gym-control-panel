{
  "name": "gym-control-panel",
  "private": true,
  "scripts": {
    "dev": "npm run dev --prefix frontend",
    "dev:all": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
